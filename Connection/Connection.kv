#: import BoxLayout kivy.uix.boxlayout
#: import Connection Connection.Connection

<Connection>:
	orientation: 'vertical'
	padding: 20
    BoxLayout:
        Button:
            text: 'Return'
            on_press: root.Root_callback()
	Button:
	    text: 'Video Library'
	    on_press: root.VideoLibrary_callback()
	Button:
	    text: 'Audio Library'
	    on_press: root.AudioLibrary_callback()
